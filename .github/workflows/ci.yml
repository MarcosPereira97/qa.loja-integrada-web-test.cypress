name: CI - E2E Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@4

      - name: Setup Node.js
        uses: actions/setup-node@4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Run Cypress tests
        uses: cypress-io/github-action@6
        with:
          browser: chrome

      - name: Upload Allure results
        if: always()
        uses: actions/upload-artifact@4
        with:
          name: allure-results
          path: allure-results
          retention-days: 7
